// (25) - Introdução a Arrays - Combinando e cortando arrays /

const primeiro = [1,2,3];
const segundo = [4,5,6];

// Combinar:
const combinado = primero.concat(segundo);
console.log(combinado);

// Cortar:
const cortado = combinado.slice(1,2);
console.log(cortado);